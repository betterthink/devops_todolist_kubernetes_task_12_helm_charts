# Validation of changes in Django ToDo app
## Installing app
```bash
./bootsrap.sh
```
## Validation changes
Run the commands to see the pods

```bash
kubectl get pods -n todoapp
```

```bash
kubectl get pods -n mysql
```